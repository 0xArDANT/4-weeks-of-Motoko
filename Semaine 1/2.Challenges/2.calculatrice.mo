/* Semaine 1, Exercice 2
 * Compléter le code suivant pour que la fonction :
 * prenne deux nombres puis un signe ("+" ou "-" ou "*" ou "/") en paramètres
 * La fonction devra effectuer sur les deux nombres l'opération indiquée par l'utilisateur
 * Elle devra retourner le résultat sous forme de nombre réel
*/

actor Calculatrice {
    public query func calc () {};
};